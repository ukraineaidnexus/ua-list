{{/* Generates one detail page per NGO entry from data/orgs.
     English only by design: entry content is English site-wide. */}}
{{ range $file, $o := site.Data.orgs }}
  {{ if $o.draft }}{{ continue }}{{ end }}
  {{ if or (eq $o.type "ua-ngo") (eq $o.type "foreign-ngo") }}
    {{ $content := dict "mediaType" "text/markdown" "value" ($o.history | default "") }}
    {{ $.AddPage (dict
      "path" $o.id
      "title" $o.name
      "kind" "page"
      "params" (dict "org" $o)
      "content" $content
    ) }}
  {{ end }}
{{ end }}
